odoo.define('pos_rksv.gui', function (require) {
    "use strict";

    var core = require('web.core');
    var gui = require('point_of_sale.gui');

    // Nothing to do here for now
    gui.Gui.include({
    });

});
